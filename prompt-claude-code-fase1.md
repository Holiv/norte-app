# Prompt de kickoff — Claude Code (Fase 1)

> Coloque `arquitetura-app-financas.md` E `CLAUDE.md` juntos na raiz da pasta
> do projeto. O `CLAUDE.md` carrega sozinho em toda sessão futura — você não
> precisa colar instrução de regra permanente de novo. Este prompt abaixo é
> só o "play" inicial, pra dar o pontapé na Fase 1.

---

Leia `CLAUDE.md` e `arquitetura-app-financas.md` (ambos na raiz) antes de
qualquer coisa.

Vamos começar a Fase 1 agora (Fundação + fluxo diário), seguindo a ordem de
construção descrita no CP6 do arquivo de arquitetura.

Antes de escrever qualquer código, me apresente:
1. Pré-requisitos que preciso ter instalado/configurado (Node.js, conta
   Supabase, conta Vercel, quais chaves/variáveis de ambiente),
2. Estrutura de pastas proposta,
3. Ordem dos primeiros commits dentro da Fase 1.

Só comece a implementar depois que eu aprovar esse plano.
